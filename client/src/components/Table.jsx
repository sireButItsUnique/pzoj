import Link from "next/link";

const Table = (props) => {
    const {status, problemTitle, solutionLink, difficulty} = props;

    return (
        <div className="flex flex-col justify-center items-center">
            {/* status */}
            <div>

            </div>

            {/* problemTitle */}
        </div>
    );
}

export default Table;